export const usersTable = (items) => {
    return items.map((item) => {
        console.log(item);

        //     return {
        //         key: key,
        //         label: item,
        //     };
    });
};
